# Fix the Compose project name so Docker network names are stable across restarts.
# Without this, Astronomer generates a random hash (e.g. airflow-test_df1758_airflow).
COMPOSE_PROJECT_NAME=themeparks

MINIO_ENDPOINT=https://minio-api.andylile.com
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=

OBJECT_STORAGE_SYSTEM=s3
OBJECT_STORAGE_CONN_ID=minio_default
OBJECT_STORAGE_PATH=airflow-data/themeparks-pipeline

# Object Storage XCom backend (apache-airflow-providers-common-io)
# XComs larger than threshold (bytes) are stored in MinIO instead of Postgres
# -1 = all in Postgres, 0 = all in object storage
# AIRFLOW__CORE__XCOM_BACKEND=airflow.providers.common.io.xcom.backend.XComObjectStorageBackend
# AIRFLOW__COMMON_IO__XCOM_OBJECTSTORAGE_PATH=s3://minio_default@airflow-xcom/xcom
# AIRFLOW__COMMON_IO__XCOM_OBJECTSTORAGE_THRESHOLD=1048576
# AIRFLOW__COMMON_IO__XCOM_OBJECTSTORAGE_COMPRESSION=zip